package project.main.specifications.base;

public enum DeleteFilterOption {
    NOT_DELETED, // Mostrar solo los no eliminados
    DELETED,     // Mostrar solo los eliminados
    ALL          // Mostrar todos, eliminados y no eliminados
}